apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ include "hello-app.name" . }}
