replicaCount: 1

image:
  repository: nginx
  tag: "latest"

service:
  type: LoadBalancer

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix
  tls: []

config:
  message: "Hello from Helm chart!"
